import React, { useContext } from 'react';
import AuthProvider, { AuthContext } from '../Provider/AuthProvider';

const Home = () => {
    return (
        <div>
        </div>
    );
};

export default Home;